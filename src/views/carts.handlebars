{{#each products}}
    <div class="cart-product">
        <h2>{{title}}</h2>
        <p>{{description}}</p>
        <p>Precio: ${{price}}</p>
        <p>Cantidad: {{quantity}}</p>
    </div>
{{/each}}